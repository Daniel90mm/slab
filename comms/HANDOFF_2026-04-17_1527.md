# Session Handoff

Phase `2.2` Slice `1` is implemented: [src/commands/createNotebookFromScaffold.js](/home/daniel/Documents/Projekter/slab-vscode/src/commands/createNotebookFromScaffold.js:1) adds `slab.createNotebookFromScaffold`, which resolves a local scaffold markdown path from an explicit URI, the active markdown editor, or an open-file dialog, parses it with the locked adapter, emits a sibling `.ipynb`, writes it atomically via [src/util/atomicWrite.js](/home/daniel/Documents/Projekter/slab-vscode/src/util/atomicWrite.js:1), and refuses to overwrite an existing notebook; [src/commands/exportNotebookForReview.js](/home/daniel/Documents/Projekter/slab-vscode/src/commands/exportNotebookForReview.js:1) adds `slab.exportNotebookForReview`, which requires a saved local active notebook, flattens the on-disk `.ipynb` with the locked adapter, and silently overwrites a sibling `<concept>-review.md`; and [src/extension.js](/home/daniel/Documents/Projekter/slab-vscode/src/extension.js:1) plus [package.json](/home/daniel/Documents/Projekter/slab-vscode/package.json:1) register both commands and their activation events. Automatic verification completed: `npm run smoke` passes across preview activation, attachment extraction, notebook flattening, and scaffold-to-notebook emission. Interactive verification status is mixed and should be carried forward explicitly: `slab.createNotebookFromScaffold` is **pending user verification**, `slab.exportNotebookForReview` is **pending user verification**, and the earlier Phase `2.0` debt for `slab.extractAttachments` on a real pasted-image notebook is still **pending user verification**; I attempted a scripted Extension Host harness for the new commands, but in this environment `code --extensionTestsPath ...` launched without producing a test callback, logs, or result file, so I am not claiming those commands passed interactively.

## Manual Verification Checklist

### 1. `slab.extractAttachments` on a real pasted-image notebook

Status: **pending user**

1. Open `/home/daniel/Documents/Projekter/slab-vscode` in VS Code.
2. Press `F5` to open an Extension Development Host.
3. In the Extension Development Host, create or open a saved local `.ipynb` notebook.
4. Add a markdown cell and paste an actual image into it so the source becomes `![...](attachment:...)`.
5. Save the notebook.
6. Run `Slab: Extract Attachments from Notebook` from the Command Palette.
7. Verify all of these:
   - the markdown cell source now references `./assets/paste-<timestamp>.<ext>`
   - an `assets/` directory exists beside the notebook
   - the extracted image file exists in `assets/`
   - the `.ipynb` no longer contains a top-level `attachments` field for that cell
8. If any part fails, record it as a separate follow-up bug; do **not** fix it inside Phase `2.2`.

### 2. `slab.createNotebookFromScaffold` on the Bayesian scaffold

Status: **pending user**

1. In the same Extension Development Host, open the file:
   `/home/daniel/Desktop/Mig/Culmen koncepter/Bayesian Learning and Priors/Bayesian Learning and Priors.md`
2. Run `Slab: Create Notebook From Scaffold`.
3. Verify:
   - a sibling file named `Bayesian Learning and Priors.ipynb` is created
   - the notebook opens cleanly in VS Code as a Python notebook
   - the first cell is a markdown title cell with `# Bayesian Learning and Priors`
   - each scaffold section becomes prompt cells plus a blank markdown answer cell
   - no overwrite prompt appears if the notebook does not yet exist
4. Re-run the command once with the notebook still present and verify it fails cleanly with an actionable error about the target already existing.

### 3. `slab.exportNotebookForReview` on the resulting notebook

Status: **pending user**

1. Open `Bayesian Learning and Priors.ipynb` in the Extension Development Host.
2. Add some text to one of the blank markdown answer cells, for example:
   `My current understanding of the posterior is ...`
3. Save the notebook.
4. Run `Slab: Export Notebook For Review`.
5. Verify:
   - a sibling file named `Bayesian Learning and Priors-review.md` is written beside the notebook
   - re-running the command overwrites that file silently
   - the exported markdown contains the title heading
   - the exported markdown contains the answer text you just added
   - code cell outputs do not appear in the exported review markdown
6. Dirty-buffer check:
   - modify the notebook again without saving
   - run `Slab: Export Notebook For Review`
   - verify it refuses with the “save before exporting review” style message

## Open Threads

- The scripted Extension Host verification harness did not run reliably in this environment: `code --extensionTestsPath` launched but produced no callback output or result file, so interactive command verification remains manual.
- No product bugs were confirmed in this phase. If the manual `slab.extractAttachments` check reveals a problem, treat it as a new scoped bugfix task instead of expanding Phase `2.2`.
