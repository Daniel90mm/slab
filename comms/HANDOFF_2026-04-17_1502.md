# Session Handoff

Phase 2.0 is implemented through the raw-file path only: [src/attachments/extract.js](/home/daniel/Documents/Projekter/slab-vscode/src/attachments/extract.js:1) now performs pure `.ipynb` attachment extraction with MIME-based filename generation, collision handling, and source rewrites; [src/commands/extractAttachments.js](/home/daniel/Documents/Projekter/slab-vscode/src/commands/extractAttachments.js:1) adds the manual `slab.extractAttachments` command with the required dirty-buffer guard, local-file guard, atomic asset writes, notebook rewrite, and rollback on failure; [src/extension.js](/home/daniel/Documents/Projekter/slab-vscode/src/extension.js:1) and [package.json](/home/daniel/Documents/Projekter/slab-vscode/package.json:1) register and expose that command; and the extractor is covered by [tests/smoke/extract-attachments.mjs](/home/daniel/Documents/Projekter/slab-vscode/tests/smoke/extract-attachments.mjs:1) plus the synthetic fixture in [tests/smoke/fixtures/with-attachment.ipynb](/home/daniel/Documents/Projekter/slab-vscode/tests/smoke/fixtures/with-attachment.ipynb:1). Verification completed: `npm run smoke` passes, and a direct Node invocation of `extractNotebookAttachments()` against a temp notebook rewrote the notebook, removed `cell.attachments`, preserved `cell.id`/`nbformat`, and materialized `./assets/paste-20260417-150405.png`; spike findings to preserve are that runtime notebook cells exposed pasted attachments at `cell.metadata.attachments`, and a throwaway `WorkspaceEdit` round-trip also removed serialized `cell.attachments` cleanly on a synthetic notebook, but those results are advisory only and do not change the Phase 2.0 decision to keep raw-file extraction as the primary path. The next exact step is a real manual check in an Extension Development Host on an actual pasted-image notebook, because I did **not** complete that interactive VS Code run in this session and should not claim it.
